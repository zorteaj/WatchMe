Steps to run:

1. `pip install -r requirements.txt`* 
2. `python main.py`
3. Follow instructions on screen to switch between optical flow and facial recognition

*I had never used Python or pip before this class, but I tested this and it seems to work.  If you have issues with dependencies you may just have to manually install dependencies with pip.

I've been running the application using Python 3.6.7 locally